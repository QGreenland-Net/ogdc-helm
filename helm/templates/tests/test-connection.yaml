apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ogdc.fullname" . }}-test-connection"
  labels:
    {{- include "ogdc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ogdc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
