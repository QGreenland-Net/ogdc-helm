apiVersion: v1
kind: Service
metadata:
  name: {{ include "ogdc.fullname" . }}
  labels:
    {{- include "ogdc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ogdc.selectorLabels" . | nindent 4 }}
